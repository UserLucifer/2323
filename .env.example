SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=dev

MYSQL_URL=jdbc:mysql://localhost:3306/compute_rental?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
MYSQL_USERNAME=root
MYSQL_PASSWORD=
MYSQL_POOL_MAX_SIZE=20
MYSQL_POOL_MIN_IDLE=5

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DATABASE=0
REDIS_PASSWORD=
REDIS_TIMEOUT=3s

RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=
RABBITMQ_PASSWORD=
RABBITMQ_VIRTUAL_HOST=/
RABBITMQ_PREFETCH=20

SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USERNAME=business@goyantrade.com
SMTP_PASSWORD=gA:#@qX;Oh74
SMTP_SSL_ENABLE=true
SMTP_STARTTLS_ENABLE=false

JWT_ISSUER=compute-rental
JWT_SECRET=replace-with-at-least-32-bytes-random-secret
JWT_ACCESS_TOKEN_TTL=2h

API_TOKEN_ENCRYPTION_SECRET=replace-with-api-token-encryption-secret
API_TOKEN_BASE_URL=https://api.example.invalid/v1

AUTH_EMAIL_CODE_TTL=5m
AUTH_EMAIL_CODE_LENGTH=6
AUTH_EMAIL_CODE_RATE_LIMIT_PER_MINUTE=5
AUTH_EMAIL_FROM=
AUTH_EMAIL_SUBJECT=Compute Rental login verification code

WEBSOCKET_ALLOWED_ORIGINS=*

SCHEDULER_DAILY_PROFIT_CRON=0 5 0 * * *
SCHEDULER_ORDER_EXPIRE_SETTLE_CRON=0 10 0 * * *
SCHEDULER_AUTO_PAUSE_CRON=0 */5 * * * *
SCHEDULER_ACTIVATION_TIMEOUT_CANCEL_CRON=0 */10 * * * *

# Dev-only default admin is initialized by src/main/resources/sql/init-data.sql:
# username=admin
# password=admin123
# Change the BCrypt password hash before production use.
